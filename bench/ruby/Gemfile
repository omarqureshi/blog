# frozen_string_literal: true

source "https://rubygems.org"

# The Ruby CDK preview: release-style build versions (0.0.0.<timestamp>), so a
# bare requirement resolves the newest build; the pinned transitive closure
# (constructs, asset packages, jsii runtime) rides in with it.
gem "aws-cdk-lib", source: "https://rubygems.omarqureshi.net"
